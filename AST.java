public class AST {
    private Token[] tree;
    public AST(Token[] tree) { this.tree = tree; }
    public Token[] getTree() { return tree; }
    public void setTree(Token[] tree) { this.tree = tree; }
}
